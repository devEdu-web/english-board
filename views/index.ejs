<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/menu.css">
    <link rel="stylesheet" href="/styles/progress-bar.css">
    <link rel="stylesheet" href="/styles/subtitles.css">
    <link rel="stylesheet" href="/styles/style.css">
    <title>Dashboard</title>
</head>
<body>
    <section class="all-page d-flex">
        <%- include('./includes/side-nav') %> 
        <section class="page-content">
            <%- include('./includes/nav-header') %> 
            <main class="d-flex flex-column data-container gap-3">
                <div class="row counters">
                    <div class="col words-counter d-flex flex-column gap-3">
                        <div class="words-wrapper d-flex justify-content-between align-items-center">
                            <h2>Words</h2>
                            <span class="fs-5 emphasis"><%= collections.wordsCollection.length %> </span>
                        </div>
                        <span>Last Register: <span class="emphasis"><%= collections.wordsCollection[collections.wordsCollection.length - 1].wordName %> </span></span>
                    </div>

                    <div class="col hours-counter d-flex flex-column gap-3">
                        <div class="words-wrapper d-flex justify-content-between align-items-center">
                            <h2>Hours</h2>
                            <span class="fs-5 emphasis"><%= collections.hoursCollection[0].hoursCounter %></span>
                        </div>
                        <span>Last Register: <span class="emphasis"><%= Object.keys(collections.hoursCollection[0].hoursInfo[collections.hoursCollection[0].hoursInfo.length - 1]) %> </span></span>
                    </div>
                </div>

                <div class="row informations">
                    <div class="col words-information d-flex flex-column gap-3">
                        <h1>Words information</h1>
                        <div class="progress">
                            <div id="nouns-bar" class="progress-bar" role="progressbar" style="width: <%= collections.wordsInfo[0].percentage%>%"><%= collections.wordsInfo[0].percentage%>%</div>
                            <div id="verbs-bar" class="progress-bar" role="progressbar" style="width: <%= collections.wordsInfo[1].percentage%>%;"><%= collections.wordsInfo[1].percentage%>%</div>
                            <div id="adverbs-bar" class="progress-bar" role="progressbar" style="width: <%= collections.wordsInfo[2].percentage%>%;"><%= collections.wordsInfo[2].percentage%>%</div>
                            <div id="adjectives-bar" class="progress-bar" role="progressbar" style="width: <%= collections.wordsInfo[3].percentage%>%;"><%= collections.wordsInfo[3].percentage%>%</div>
                            <div id="phrasal-verbs-bar" class="progress-bar" role="progressbar" style="width: <%= collections.wordsInfo[4].percentage%>%;"><%= collections.wordsInfo[4].percentage%>%</div>
                        </div>

                        <div class="words-subtitle d-flex flex-wrap gap-4">
                            <div class="words-subtitle-item d-flex align-items-center gap-1" id="nouns-subtitle">
                                <div class="subtitle-dot" id="nouns-dot"></div>
                                <span>Nouns: <%= collections.wordsInfo[0].quantity%></span>
                            </div>

                            <div class="words-subtitle-item d-flex align-items-center gap-1" id="nouns-subtitle">
                                <div class="subtitle-dot" id="verbs-dot"></div>
                                <span>Verbs: <%= collections.wordsInfo[1].quantity%></span>
                            </div>

                            <div class="words-subtitle-item d-flex align-items-center gap-1" id="nouns-subtitle">
                                <div class="subtitle-dot" id="adverbs-dot"></div>
                                <span>Adverbs: <%= collections.wordsInfo[2].quantity%></span>
                            </div>

                            <div class="words-subtitle-item d-flex align-items-center gap-1" id="nouns-subtitle">
                                <div class="subtitle-dot" id="adjectives-dot"></div>
                                <span>Adjectives: <%= collections.wordsInfo[3].quantity%></span>
                            </div>

                            <div class="words-subtitle-item d-flex align-items-center gap-1" id="nouns-subtitle">
                                <div class="subtitle-dot" id="phrasal-verbs-dot"></div>
                                <span>Phrasal Verbs: <%= collections.wordsInfo[4].quantity%></span>
                            </div>
                        </div>
                    </div>

                    <div class="col hours-information  d-flex flex-column gap-3">
                        <h1>Hours information</h1>
                        <div class="progress">
                            <div id="year-bar" class="progress-bar" role="progressbar" style="width: <%= collections.hoursInfo.hoursPercentage.daysPercentage %>%;"><%= collections.hoursInfo.hoursPercentage.daysPercentage %>%</div>
                            <div id="hours-bar" class="progress-bar" role="progressbar" style="width: <%= collections.hoursInfo.hoursPercentage.listeningPercentage %>%;"><%= collections.hoursInfo.hoursPercentage.listenigPercentage %>%</div>
                        </div>

                        <div class="hours-subtitle d-flex flex-wrap gap-4">
                            <div class="hours-subtitle-item d-flex align-items-center gap-1">
                                <div class="subtitle-dot" id="year-dot"></div>
                                <span>Days of the year</span>
                            </div>

                            <div class="hours-subtitle-item d-flex align-items-center gap-1">
                                <div class="subtitle-dot" id="hours-dot"></div>
                                <span>Days listening</span>
                            </div>
                        </div>

                    </div>
                </div>
            </main>
        </section>
    </section>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

</body>
</html>